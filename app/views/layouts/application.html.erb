<!DOCTYPE html>
<html>
<head>
  <title>Leader</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'content', media: 'all', 'data-turbolinks-track': 'reload' %>
</head>
  <body>
    <%# <div>
      <%= render 'shared/leader' %>
    <div>
      <%= render 'shared/sidebar' %>
    </div>
          <%# <div>
            <%= render 'shared/navbar' %>
          <%# </div>  %>
    <div>
      <%= yield %>
    </div>
  </body>
</html>
